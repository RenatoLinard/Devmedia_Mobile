import React from "react";
import { View, TextInput } from "react-native";

//TextInput é um componente nativo import da biblioteca react-native
//é utilizado apenas uma tag de abertura e fechamento indicando que nenhum 
//componente será inserido nele.
/* Possui propriedades para estilzar sua aparencia:

textAlign: alinhamento da caixa de texto

keyboardType: estilo do teclado que será exibido

autoFocus: Ao carregar a tela o foco será neste componente caso esteja com valor
boleano: true

maxLength: Indica a quantidade de caracters que este campo pode receber

value: Caso atribuido um valor deve ser uma variavel de estado para 
depois utilizar o evento onChangetext
*/

//Evento: è o resultado de ação que vai gerar uma reação no caso chamar uma 
//função

/*
    onChangetext= {valor => setValorMinino(valor)}
*/

const Conceitos = () => {
    return(
        <View>
            
        </View>
    )
}
 export default Conceitos;
